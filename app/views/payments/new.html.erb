<h1>Purchase from Casual <%= @purchase.item_name %></h1>
<%= form_tag purchase_payments_path(@purchase) do %>
  <article>
    <label class="amount">
      <span>Amount: <%= humanized_money_with_symbol(@purchase.amount) %></span>
    </label>
  </article>


  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
    data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-name="Casual"
    data-email="<%= current_user.email %>"
    data-description="Casual <%= @purchase.item_name %>"
    data-amount="<%= @purchase.amount_cents %>"
    data-currency="<%= @purchase.amount.currency %>"></script>

<% end %>
